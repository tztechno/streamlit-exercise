pip install streamlit

python -m venv .venv

source .venv/bin/activate

streamlit run app.py

—————————

git init

git add .

git commit -m "Initial commit"

git remote add origin https://github.com/tztechno/streamlit-parts

git push -u origin master
